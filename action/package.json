{
  "name": "oven-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action for running oven agent pipelines",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts --source-map --license licenses.txt",
    "test": "vitest run",
    "lint": "eslint src/ __tests__/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "@actions/tool-cache": "^2.0.2"
  },
  "engines": {
    "node": ">=23.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.12",
    "@vercel/ncc": "^0.38.3",
    "eslint": "^9.17.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
